# Review pipeline agent for this repository only.
#
# The global configuration runs the pipeline on codex. This repository overrides
# that to Claude so its validation rounds use the configured included Opus model
# while Cursor capacity is exhausted. Model and effort configuration is
# intentionally absent because no-mistakes supports those settings only in its
# machine-local global configuration, not in repository configuration.
#
# Added 2026-09-21 on the captain's instruction as a temporary measure. Revisit
# rather than assume it is permanent.
agent: [claude]
